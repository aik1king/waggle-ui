一定來不及放在SERVER的,只能從client下手
缺點是這個cache太大時,會有問題
不過,如果格子cache逐步更新,傳回來的數值和以往不同,而且屬於同一個TYPE的會自動消除,就會逐步減少內容
SERVER那邊,拿到一組之後,每個項目只保留最後一個,和先前合併比較
一陣計算後,會更新許多數值
資料應該會放在local,過一陣子會刪除舊資料,讀取新資料



todo

UI
	移動機制
		有一個cache叫做全部亂七八糟
		從loc移除內容除了放進fav外,也要把自己放進cache中
		( 如果本來就有的話,可能是不同type要移除,總之一個名子只能出現一次 )
		
		畫面移動時,都會先啟動這個cache機制,用這個cache內容繪圖
		其他格子畫圖時,檢查這個cache中,如果在別人type,自己就不畫出來
		如果和自己同type,表示又跑回來了,沒有必要保留,可以移除

	客戶登入機制

SERVER
	讀取
		不需要每次讀取s3數值,放在local filesystem當作cache,過一陣子自動更新
	ip2geo機制還沒完成




parser

製作一個流程,讀取檔案
更新大檔案
進行index計算
進行推薦更新,計算,更新檔案
